
public interface LListInterface
{
    //input : string data to put into linked list
    //output : null
    public abstract void add(String input);

    //input : string data to remove from linked list
    //output : linked list without that string
    public abstract boolean remove(String input);

    public static void main(String[] args)
    {
        //Receive string as outside input
        //Push it into linkedlist with push()
        //Pop string at top of list with pop()
    }
}
